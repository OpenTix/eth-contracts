{
  "name": "eth-contracts",
  "version": "1.0.0",
  "description": "Repo that contains all of our smart contracts",
  "main": "soljson-v0.8.28+commit.7893614a.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "rebuild": "npx hardhat clean && npx hardhat compile",
    "build": "npx hardhat compile",
    "clean": "npx hardhat clean",
    "deploy_mint": "npx hardhat ignition deploy ./ignition/modules/VenueMint.ts --network localhost",
    "local_console": "npx hardhat console --network localhost",
    "generate_abi": "solc-windows.exe --base-path . --include-path node_modules\\ contracts\\VenueMint.sol --abi"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.16"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
